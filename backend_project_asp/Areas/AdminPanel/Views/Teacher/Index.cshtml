@{
    ViewData["Title"] = "Index";
    List<Teacher> teachers = Model;
}



<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Teachers</h4>
                <a asp-controller="Teacher" asp-action="Create"> Create Teacher</a>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Image</th>
                                <th>Name</th>
                                <th>Position</th>
                             
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in teachers)
                            {
                                <tr>
                                    <td><img width="150" height="150" src="~/img/teacher/@item.Image" /></td>
                                    <td>@item.Name</td>
                                    <td>@item.Position.PositionName</td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-success">Detail</a>
                                        <a asp-action="Update" asp-route-id="@item.Id" class="btn btn-warning">Update</a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>







